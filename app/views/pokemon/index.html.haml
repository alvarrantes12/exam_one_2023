%p{:style => "color: green"}= notice
.div.m-4
  .page.header
    %h1.mb-4
      =t('application.list_of', model:t('activerecord.modules.pokemon.other'))

  %table.table.table-striped
    %thead
      %tr
        %th= t('activerecord.attributes.pokemon.name')
        %th
        %th

      - @pokemon.each do |pokemon|
        %tr
          %td= pokemon.name
  %br

